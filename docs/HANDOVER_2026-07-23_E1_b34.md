# TRTM Handover - 2026-07-23 (E1-b34; lot-weighted anchor - SEALED).
# Follow CLAUDE.md + the staged-delivery gates. This file + STATE.md are
# truth. Disk + git override conversation/auto-memory.

## 1. RESUME PROTOCOL (first actions, in order)
1. Run all four, compare to STATE.md:
   - git status (tree clean, or Jeff explains)
   - sha256sum src/TRTM.mq5 | cut -c1-16   EXPECT aef5dc989609dc45
   - wc -l src/TRTM.mq5                      EXPECT 4307
   - sha256sum the MT5 runtime copy (path in CLAUDE.md section 0)
   All four match = say "aligned" in one line. Mismatch = STOP, ask which
   copy produced the live evidence (the MT5 copy did).
2. Hygiene after any edit: CRLF + ASCII-only; brace baseline is -1
   (compare DELTA only) - b34 count is 409 { / 410 }. 0 bare LF.
3. Read section 3. Do not re-plan sealed work or re-litigate locked
   decisions.

## 2. WHERE THE PROJECT STANDS
- CORE COMPLETE (sealed): Stages 1-7, Stage 8 Step 1, Stage 9 Steps 1-2,
  Stage 10 observability. Full live loop built + demo-verified.
- ENHANCEMENT E1 SEALED this session at E1-b34 (2026-07-23): the sequence
  anchor is now the LOT-WEIGHTED average of open entries (was simple
  mean) across avg-TP, BE stop/trigger, trail activation/trigger, AND the
  dashboard projection/avg-entry row. Recovery (lot sizing + spacing),
  the lowest-level SL anchor, CostCoverPoints, and the manual-exit path
  are UNCHANGED. Equal-lot sequences are bit-identical to b33.
- Gates cleared in order: Gate 1 (Option A - lot-weighted, all paths;
  rejected B TP-only and C keep-simple, rationale in STATE.md locked
  decisions) -> Gate 2 (E1_MATRIX.md, 25 rows) -> Gate 3 (E1_PLAN.md)
  -> Gate 4 (b34) -> Gate 5 (E1_CHECKLIST.md all PASS, 5 live runs) ->
  Gate 6 seal by Jeff.
- One build: b34. +11 lines (4296 -> 4307). Compiled clean 0/0.

## 3. NEXT SESSION - queued items (Jeff picks order)
- E4 (Drawdown Reduction Tier 1) is now UNBLOCKED - E1 (its dependency)
  has landed. Opens with its own Gate 1 (money-path). All OBSERVED/CHOSEN
  detail + open sub-decisions O1-O5 are in STATE.md's backlog and
  docs/ENHANCEMENT_INPUT_2026-07-23_tier1.md.
- E2 Stage 8 Step 2 (draggable EXIT lines LIVE) - not built.
- E3 Stage 9 Step 3 (auto-entry stub, MQL_TESTER-gated) - reuse OFFSET seed.
- E5/E6 (Tiers 2/3) - need reference runs first (E7 R1/R2). E7 is a
  research task, not a build.
- FINDINGS still open: F3 (Shadow close-deals-as-initial defect class -
  guardrail note only), F4 (Shadow BE unobserved - design note). F1/F2
  RESOLVED this session.

## 4. E1 SCOREBOARD (audited to the cent; seal evidence)
5 live demo runs on XAUUSD.s M1, all recomputed weighted vs simple:
- Run 1 (unequal BUY, incremental 0.01, 10 levels 0.01->0.10): every
  multi-level TP = weighted avg + 200 pts exact (L10 4124.88 weighted vs
  4125.81 simple); BE fired "avg 4122.88" (= weighted) SL 4123.18 =
  weighted+30; basket exited on weighted TP. S1/S5/S7/S10/S11/S12/S14/
  S15/S16/S18 PASS.
- Run 2 (equal SELL, RM_FIXED 0.02, 10 levels): weighted==simple, every
  TP = mean-200 exact; SELL BE avg 4124.88, SL 4124.58 = avg-30 (sign
  correct). S3/S4 PASS - the equal-lot / martingale-stall no-op class.
- Run 3 (trail, unequal BUY, TP off): TRAILING ACTIVATED @ 4058.89;
  threshold = weighted 4056.8207 + 180 = 4058.62; simple threshold 4059.02
  would NOT have fired -> weighted confirmed. Ratchet correct. S13 PASS.
- Run 4 (manual TP edit, unequal BUY): Manual TP 4047.40 ADOPTED (path
  unchanged); on L4 add RELEASED, computed re-asserted = weighted 4046.01
  (not simple 4046.33). S17 PASS.
- Run 5 (hard-kill + re-anchor): unclean shutdown proven (lock re-assert
  branch, OnDeinit skipped); reconcile recomputed anchor from live
  positions (no persisted average - not in state schema); closed L1
  (lowest), survivors' TP 4044.95->4044.84 = weighted removal exact
  (simple would be ~4045.04). S22/S23/S24 PASS.
- EQUIVALENCE: S2 (equal BUY), S6 (unequal SELL), S8 (descending), S9
  (manual-list) - the averaging code has NO direction term and NO order
  term; Run 1 proved arbitrary weights diverge, Run 2 proved both signs.
- INSPECTION: R-1/R-2/R-3 - recovery lots opened exactly per config,
  50-pt spacing held, SL stayed at the lowest-level anchor throughout.
No FAILs. No findings. No new parked items.

## 5. EMPIRICAL FACTS LEDGER (terminal is truth; do not relearn)
NEW / re-confirmed this session:
- The BE-anchor "simple vs lot-weighted" parked item is now CLOSED:
  lot-weighted shipped and sealed (E1-b34). The old b33 empirical note
  "BE uses simple mean" is SUPERSEDED - do not cite it as current.
- Gold XAUUSD.s: _Point 0.01, contract x100 (0.01 lot = $1 per $1 move);
  SYMBOL_TRADE_TICK_VALUE = $1.00 for 1.0 lot. Projection pTP/pSL =
  100 * sum(vol*(target-entry)); verified against +6.00/+110.24/+20.04.
- XAUUSD.s stops level read 100 pts at the 22:03 and 22:11 inits (within
  the DYNAMIC 20-100 band; the b27 BE-geometry WARN fired correctly at
  Trigger100-Offset30=70 < 100).
CARRIED (still true): Doo Prime XAUUSD.s stops DYNAMIC ~20-100 pts intraday
(Environment note reworded this session, F2 resolved); Task Mgr "End task"
is graceful (invalid for a true kill) - Run 5 used it and the log DID show
the unclean-shutdown lock re-assert, so on this build End Task skipped
OnDeinit (note: contradicts the older "End task = graceful" ledger line -
worth a re-check if kill-cleanliness matters again); MQL_TRADE_ALLOWED
reads true at OnInit.

## 6. FILE MANIFEST (current truth set)
- src/TRTM.mq5              E1-b34  aef5dc989609dc45  4307 lines
- STATE.md                  b34 header; b34 change section SEALED; E1
                            locked-decision (Option A + scope correction);
                            F1/F2 resolved, F3/F4 open; E4 unblocked
- docs/E1_MATRIX.md         25 rows, 6 groups; SEALED 2026-07-23
- docs/E1_PLAN.md           2 touch points (helper removed at build);
                            delivered
- docs/E1_CHECKLIST.md      S0-S24; all PASS; 5-run disposition
- docs/ENHANCEMENT_INPUT_2026-07-23_tier1.md  E4-E7 input (Gate 1 input)
- README.md                 core-concepts "Average entry (lot-weighted)"
                            + Exits table updated this session
- prior sealed docs (STAGE8/9/10, handovers) unchanged
- ECOSYSTEM_BACKLOG.md      never re-import into TRTM

## 7. TODO for Jeff (carry into repo)
- Deploy note: E1-b34 was copied to the MT5 Experts tree and run live this
  session (evidence is from that copy). Keep repo = MT5 in sync; git wins
  on any mismatch.
- Optional: if you ever want S22 airtight to the cent, the pre-kill
  3-level entries from the 22:11 run would let it be a literal pre/post
  match (sealed on composition + the S24 discriminator instead).

## 8. WORKING AGREEMENTS (binding; from CLAUDE.md)
Gate order: locked decisions -> sealed matrix (money paths) -> confirmed
plan -> surgical build -> evidence-audited verification -> seal on Jeff's
explicit word. One question per message. No silent paths. Terminal is
truth; recompute every money number before PASS; absence items need
explicit absence checks. STATE.md ships with EVERY build; bump TRTM_BUILD
every delivery. Repo src/TRTM.mq5 is the master; MT5 tree is deploy-only
(Jeff's manual step, never written by Claude Code). Money-behavior changes
need explicit confirmation however small. Chat concise; traces + rationale
go in the artifact files.
